number( строка приводится к  числу)
number ( булиан воспринимается как ноль и единица)
number ( строка приводится к числу)
string ( числа приводятся к строкам и конкатенируются)
string( числа приводятся к строкам и конкатенируются)
boolean (массив приводится к булину)
string (конкатенация строк)
boolean ( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
boolean( все типы приводятся к булину в случае логических операций)
string (все приводится к строке и конкатенируется)
boolean( все типы приводятся к булину в случае логических операций)
string(все приводится к строке и конкатенируется)
string(все приводится к строке и конкатенируется)
number( приведение к числу)
string( приведение к строке)
